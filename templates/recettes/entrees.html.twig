{% extends 'base.html.twig' %}
{% block title %}Entrees{% endblock %}

{% block body %}
    <h1>Entrees</h1>
    <section class="entrees">
        {% for recette in entrees %}
            <a href="{{path('app_detail', {'id': recette.id}) }}">{{ recette.nom }}</a>
        {% endfor %}
    </section>
{% endblock %}


